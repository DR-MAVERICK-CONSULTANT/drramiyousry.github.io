# دليل ربط نموذج التواصل بـ Google Sheets + تفعيل لوحة المتابعة (Dashboard)

هذا الدليل يشرح كل خطوة بالتفصيل. المدة المتوقعة: 10-15 دقيقة.

---

## الخطوة 1 — إنشاء Google Sheet

1. افتح https://sheets.google.com وأنشئ ملف جديد (Blank spreadsheet).
2. سمّيه مثلاً: **Website Inquiries — Dr. Ramy**.
3. من القائمة العلوية: **Extensions → Apps Script**.
4. سيفتح محرر كود جديد فيه ملف اسمه `Code.gs` بكود افتراضي (`function myFunction(){}`).
5. احذف كل الكود الموجود، وافتح ملف **Code.gs** المرفق معك في هذا المشروع، وانسخ محتواه كامل، ولصقه في المحرر.

## الخطوة 2 — المفتاح السري (Secret Key) ✅ جاهز بالفعل

المفتاح السري متسجّل بالفعل في `Code.gs` و `admin.html` بالقيمة:

```js
const SECRET_KEY = 'DRRAMY';
```

لا تحتاج تعمل أي شيء في هذه الخطوة — فقط تأكد إنك بتستخدم نسخة `Code.gs` المرفقة كما هي.

⚠️ **ملاحظة أمان:** هذا المفتاح قصير ومتوقَّع نسبيًا. كافٍ لمنع الزوار العاديين من فتح لوحة المتابعة، لكنه ليس قويًا بمعنى تشفيري حقيقي. إذا رغبت لاحقًا في رفع مستوى الحماية، غيّره لقيمة أطول وأكثر عشوائية في **الملفين معًا** (يجب أن يبقيا متطابقين تمامًا).

اختياري: لو عايز إشعار إيميل فوري مع كل رسالة جديدة (بالإضافة للتسجيل في الشيت)، ضع إيميلك في:
```js
const NOTIFY_EMAIL = 'cyber.security.consultant2030@gmail.com';
```

## الخطوة 3 — تشغيل الإعداد الأولي

1. من القائمة العلوية في محرر الكود، اختار الدالة **initialSetup** من القائمة المنسدلة بجانب زر ▶ Run.
2. اضغط **Run**.
3. أول مرة، Google هيطلب صلاحيات (Authorization required) → اضغط **Review permissions** → اختار حسابك → لو ظهرت شاشة "Google hasn't verified this app" اضغط **Advanced** ثم **Go to [project name] (unsafe)** ثم **Allow**. هذا طبيعي لأن الكود بتاعك الشخصي وغير مُراجَع من Google، وطالما إنت الوحيد المستخدم له فهو آمن.
4. رجّع لملف Google Sheet، هتلاقي شيت جديد اسمه **Submissions** فيه صف عناوين (Timestamp, Name, Email, Phone, Interest, Message, Status).

## الخطوة 4 — نشر الكود كـ Web App

1. في محرر Apps Script، اضغط **Deploy → New deployment**.
2. اضغط على أيقونة الترس ⚙️ بجانب "Select type" واختار **Web app**.
3. اضبط:
   - **Execute as:** Me (حسابك)
   - **Who has access:** Anyone
4. اضغط **Deploy**.
5. هيطلب صلاحيات تاني، وافق عليها.
6. هتظهر لك **Web app URL** بشكل مثل:
   ```
   https://script.google.com/macros/s/AKfycb.../exec
   ```
7. **انسخ هذا الرابط بالكامل** — ستحتاجه في الخطوة القادمة.

## الخطوة 5 — ربط الموقع بالرابط

افتح ملف **script.js** وابحث عن السطر:

```js
const SHEET_WEBAPP_URL = 'PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL_HERE';
```

واستبدله بالرابط اللي نسخته:

```js
const SHEET_WEBAPP_URL = 'https://script.google.com/macros/s/AKfycb.../exec';
```

## الخطوة 6 — ربط لوحة المتابعة (admin.html)

افتح ملف **admin.html** وابحث عن السطر:

```js
const WEBAPP_URL = 'PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL_HERE';
```

استبدله برابط الخطوة 4 (السطر التالي `const SECRET_KEY = 'DRRAMY';` سيبه كما هو، متطابق بالفعل مع Code.gs).

وغيّر أيضًا كلمة السر التالية لأي كلمة تحبها (هي كلمة الدخول لصفحة لوحة المتابعة نفسها):

```js
const DASHBOARD_PASSWORD = 'ramy2026';
```

## الخطوة 7 — رفع كل التعديلات على GitHub

ارفع الملفات دي (استبدال) على الريبو:
- `script.js` (معدّل)
- `admin.html` (ملف جديد)

استنى الـ Deploy يخلص (تبويب Actions).

## الخطوة 8 — التجربة

1. افتح موقعك وجرّب تبعت رسالة من نموذج "Register Your Details".
2. افتح Google Sheet بتاعك — هتلاقي صف جديد ظهر فورًا.
3. افتح: `https://dr-maverick-consultant.github.io/drramiyousry.github.io/admin.html`
4. اكتب كلمة السر اللي حددتها في الخطوة 6 → هتشوف الرسالة، مع نقطة خضراء ✅ وعداد "1 new" أعلى الصفحة.
5. اضغط "✓ Mark as read" لتعليمها كمقروءة.

---

## ملاحظات أمان مهمة

- صفحة `admin.html` محمية بكلمة سر بسيطة من جهة المتصفح فقط (client-side). هذا **يمنع الزوار العاديين**، لكنه **ليس حماية قوية**: أي شخص يفتح كود مصدر الصفحة (View Source) سيرى كلمة السر والمفتاح السري. **لا تستخدم هذه الصفحة لبيانات حساسة جدًا.**
- الرابط المباشر لصفحة `admin.html` غير موجود في أي مكان بالموقع أو القائمة، لذلك لن يظهر لزوار الموقع العاديين، لكنه يبقى قابلاً للوصول لمن يعرف الرابط بالضبط.
- لحماية أقوى فعليًا، الخيار الأنسب هو نقل هذا الجزء لموقع بخلفية (backend) حقيقية مع تسجيل دخول Google أو كلمة سر مشفّرة من جهة السيرفر — وهو خارج نطاق استضافة GitHub Pages الثابتة.
- Formspree لسه موجود في الفورم كـ backup ثانوي اختياري (إذا كان لسه مفعّل بـ Form ID حقيقي) — الرسالة هترسل لِلاثنين مع بعض لو الاتنين متفعّلين.

## حدود الاستخدام المجاني (Google Apps Script + Sheets)

- Google Apps Script للحسابات الشخصية (Gmail) بيسمح بحد كبير من الاستدعاءات اليومية (يتجاوز بمراحل حد Formspree الشهري)، وهو كافٍ جدًا لموقع بورتفوليو/استشارات.
- Google Sheets نفسه يستوعب آلاف الصفوف دون مشاكل أداء ملحوظة لهذا النوع من الاستخدام.
